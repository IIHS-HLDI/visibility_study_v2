#include <memory>
#include <tuple>

#include <glad/glad.h>

// namespace ImageLoader {

//     enum Status : uint8_t {
//         GOOD,
//         INVALID_PATH,
//         INVALID_FILE,
//         UNSUPPORTED_FILE_TYPE,
//     };

//     typedef struct _ImageTexture{
//         GLuint texture;
//         int width;
//         int height;
//     }ImageTexture;

//     inline bool fileExists(const std::string& filePath);

//     std::tuple<ImageTexture, Status> loadImageAsTexture(const std::string filePath);

//     std::tuple<ImageTexture, Status> loadThumnailAsTexture(const std::string filePath);

// }
